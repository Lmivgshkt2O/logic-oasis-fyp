# Non-secret template. Copy to .env.logic-oasis-fyp (gitignored) with
# operator-supplied values generated from the selected release manifest; never
# commit real project values. The emulator and Functions runtime read these.
FORUM_MODEL_EVIDENCE_MODE=controlled_demo
FORUM_RUNTIME_CODE_REVISION=<sha256-of-the-selected-release-code-revision>
